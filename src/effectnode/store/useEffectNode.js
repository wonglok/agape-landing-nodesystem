import create from 'zustand'
export const useEffectNode = create((set, get) => {
  //
  return {
    passArray: [],
    setPassArray: (v) => {
      set({ passArray: v })
    },

    //
  }
})

//

//
